using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreateBoard : MonoBehaviour
{
    // Referencing Cell Prefab
    // public GameObject Cell;
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("In board script");
        // Instantiating Cell Object
        // GameObject myFirstCell = (GameObject)Instantiate(Cell, new Vector3(0, 0, 0), Quaternion.identity);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
